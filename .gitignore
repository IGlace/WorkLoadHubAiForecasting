# Superpowers working directories
.superpowers/
.worktrees/

# Local-only Claude Code settings
.claude/settings.local.json

# OS / editor
.DS_Store

# Rendered reports (regenerate from the HTML)
docs/reports/**/*.pdf

# Java module: build output, jqwik state, local databases, real-mode seed output (personal data)
server/**/target/
server/**/.jqwik-database
server/**/*.db
server/**/*.db-journal
*seeded*.json
*seeded*.sql
workloadhub_export*.json
# ...except the one seeded file that is committed on purpose: forecast-core's test fixture, which is
# synthetic (no real identity) and is what the gate reads instead of generating the seed in process.
!server/forecast-core/src/test/resources/fixtures/seeded-rows.sql

# Python caches of the tools test
__pycache__/
.pytest_cache/
